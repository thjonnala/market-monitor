# Production API base URL — consolidated Thiru apps API (Azure App Service).
# Public endpoint URL, not a secret. Market Monitor lives under /api/mm.
VITE_API_BASE_URL=https://thiru-apps-api.azurewebsites.net/api/mm
