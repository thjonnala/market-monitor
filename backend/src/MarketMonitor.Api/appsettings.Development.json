{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  "Database": {
    "Provider": "SqlServer"
  },

  "ConnectionStrings": {
    "DefaultConnection": "Server=.\\SQLEXPRESS;Database=MarketMonitor;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },

  "Jwt": {
    "//": "Dev-only signing key so the app runs out of the box. Override locally with: dotnet user-secrets set \"Jwt:SigningKey\" \"<your-random-32+char-key>\". Use an env var in production.",
    "SigningKey": "dev-only-insecure-signing-key-change-me-please-32chars"
  },

  "MarketData": {
    "//": "Leave ApiKey empty to run on deterministic mock data. Recommended for full live data (charts + signals): get a free Twelve Data key at https://twelvedata.com and run: dotnet user-secrets set \"MarketData:ApiKey\" \"<your-twelve-data-key>\". To use Finnhub instead (live quotes only), also set Provider to \"Finnhub\".",
    "Provider": "TwelveData",
    "ApiKey": ""
  }
}
